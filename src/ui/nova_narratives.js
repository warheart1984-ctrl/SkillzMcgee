/**
 * Nova-style poetic-technical narratives and replay transcripts.
 */

/**
 * @param {object} [opts]
 * @returns {string}
 */
export function renderCognitiveLoopReplay(opts = {}) {
  const threadId = opts.threadId ?? "T982";
  return `COGNITIVE LOOP REPLAY — THREAD ${threadId}

[00:00]  Intent crystallizes: unify the runtime.
[00:03]  Law-spine rotates; CKCE‑1 binds to corridor.
[00:06]  Mission corridor lights: cyan → violet.
[00:09]  Continuity waves stabilize at 3s rhythm.
[00:12]  Drift vector detected (0.03) — within tolerance.
[00:15]  Operator action: workspace re‑anchoring.
[00:18]  Receipts flow; lineage nodes illuminate.
[00:21]  Tension index drops to 0.12.
[00:24]  Governance stance updates: S0 → S1.
[00:27]  Cockpit coherence achieved.
[00:30]  Loop closes. System state: EMERGENT.

Replay complete.
`;
}

/**
 * @param {object} [opts]
 * @returns {string}
 */
export function renderWaveformCollapseNarrative(opts = {}) {
  const threadId = opts.threadId ?? "T982";
  return `WAVEFORM COLLAPSE EVENT — THREAD ${threadId}

The rhythm falters.

Continuity waves, once steady at 3 seconds, distort into jagged peaks.
Amplitude surges. Phase drifts. The corridor lights flicker violet → red.

A fault blooms in the lineage graph — a hollow node where a certainty should be.

The law-spine strains. CKCE‑1 pulses gold, then dims.
Receipts stutter. Drift vectors multiply.

The organism hesitates.

Operator presence detected.
Stance strip flashes: S1 → S2 → S3.

Override binds. The waveform stabilizes.
Amplitude falls. Phase realigns. The corridor breathes again.

Collapse averted.
Continuity restored.
`;
}
